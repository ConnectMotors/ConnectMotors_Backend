spring.application.name=ConnectMotors

# PostgreSQL Configuration
spring.datasource.url=jdbc:postgresql://localhost:5432/ConnectMotors
spring.datasource.username=postgres
spring.datasource.password=postgres
spring.datasource.driver-class-name=org.postgresql.Driver

# JPA Configuration
spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=true
spring.jpa.properties.hibernate.dialect=org.hibernate.dialect.PostgreSQLDialect
spring.jpa.properties.hibernate.format_sql=true
spring.jpa.properties.hibernate.dialect=org.hibernate.dialect.PostgreSQLDialect

# Diretório para armazenar as imagens
file.upload-dir=/path/to/your/upload/directory

# JWT Configuration
jwt.secret=chaveSecretaSuperSeguraParaAssinaturaDoToken123456789
jwt.expiration=3600000

